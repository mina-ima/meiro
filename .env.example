# Client-side WebSocket endpoint (必須)
# Local development example
VITE_WS_URL=ws://localhost:8787
# Production builds are configured via Cloudflare Pages env vars:
# VITE_WS_URL=wss://meiro-server.minamidenshi.workers.dev

# Cloudflare account details for wrangler deploys
CF_ACCOUNT_ID=
CF_WORKERS_API_TOKEN=
CF_PAGES_API_TOKEN=
CF_PAGES_PROJECT=

# Optional: local wrangler CLI token (not required in CI)
CF_API_TOKEN=

# Optional observability endpoints
SENTRY_DSN=
